[project]
name = "simple-transcribe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pyaudio>=0.2.14",
    "python-dotenv>=1.1.0",
    "sounddevice>=0.5.2",
    "websockets>=15.0.1",
]
